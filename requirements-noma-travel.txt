# Noma-Travel installs. `pes` still uses a local path until `Noma-Travel/pes` exists.
# Usage: pip install -r requirements-noma-travel.txt

# git+https://github.com/Noma-Travel/backend.git@main#egg=noma-mod&subdirectory=package
