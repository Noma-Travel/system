# Non-secret defaults for env:staging (from zappa_settings_staging.json + STAGING_GUIDE)
WL_NAME: Noma Staging
URL_PREFIX: noma_staging
BASE_URL: ""
DOC_BASE_URL: https://2r4dlx8qdj.execute-api.us-east-1.amazonaws.com/noma_staging
FE_BASE_URL: https://staging.travelwithnoma.com
APP_FE_BASE_URL: https://staging.travelwithnoma.com
API_GATEWAY_ARN: arn:aws:execute-api:us-east-1:158711196499:2r4dlx8qdj
SYS_ENV: noma_staging
AGENT_LANGUAGE: en
NOMA_ROLE_ENFORCEMENT: "on"
NOMA_APPROVAL_ENFORCEMENT: "on"
# Legado; o boot ignora (Z8-A). Staging may still set "1"; that is a no-op.
NOMA_OWN_APP: "1"
# Z6 PR E: unmount app_chat after /v1/chat cutover (all nine Renglo blueprints).
NOMA_UNMOUNT_BLUEPRINTS: "blueprint,search,state,session,docs,data,schd,auth,chat"
# DATA_API_RING_POLICY is intentionally absent. The rings the /v1 resources own
# are declared by noma/api/{trips,payment_methods}.py and applied in
# noma.api.register(), so they are closed whether or not this file says so.
# Setting it here too would be a second source of truth that can drift.
# Add it only to close a ring no resource owns; it can never open one.
SYSTEM_ADMIN_EMAILS: antoniojardim@travelwithnoma.com,jpcastroneves@travelwithnoma.com
SYSTEM_ADMIN_USER_IDS: 5bd438a5e,18f25b74f
NOMA_E2E_SEED_ALLOWED: "1"
DYNAMODB_ENTITY_TABLE: noma-staging_entities
DYNAMODB_BLUEPRINT_TABLE: noma-staging_blueprints
DYNAMODB_RINGDATA_TABLE: noma-staging_data
DYNAMODB_REL_TABLE: noma-staging_rel
DYNAMODB_CHAT_TABLE: noma-staging_chat
COGNITO_REGION: us-east-1
COGNITO_USERPOOL_ID: us-east-1_vBbXLDESt
COGNITO_APP_CLIENT_ID: 6rcfm5lsscs5ocnlu4ftukdbjr
COGNITO_CHECK_TOKEN_EXPIRATION: "True"
PREVIEW_LAYER: "2"
S3_BUCKET_NAME: noma-staging-42067270
WEBSOCKET_CONNECTIONS: https://1qefn6vt95.execute-api.us-east-1.amazonaws.com/production
ALLOW_DEV_ORIGINS: "true"
INVITE_FROM: Noma <noreply@email.travelwithnoma.com>
SES_INVITE_SENDER: noreply@email.travelwithnoma.com
OPS_LINK_BASE_URL: https://staging.travelwithnoma.com
VITE_EXTENSIONS: schd,data,backend
VITE_WL_LOGO: /small_logo.jpg
VITE_WL_LOGIN: /large_logo.jpg
VITE_DEV_MODE: "true"

# ── Flight-change negotiation agent ──────────────────────────────────────────
# Runbook: extensions/backend/docs/AGENTE_TROCA_DE_VOO.md
# Secrets (RESEND_API_KEY, RESEND_WEBHOOK_SECRET, SLACK_WEBHOOK_FLIGHT_CHANGE)
# do NOT live here — see config/README.md.
FLIGHT_CHANGE_INBOUND_DOMAIN: email.travelwithnoma.com
# Must sit on the Resend-verified SENDING domain (the email. subdomain).
# The parent travelwithnoma.com has no Resend DKIM — its MX is Microsoft 365.
FLIGHT_CHANGE_EMAIL_FROM: Noma Travel <noma@email.travelwithnoma.com>
FLIGHT_CHANGE_PROVIDER_EMAIL: br1.nac@rexturadvance.com.br
# TODO: set a TEAM alias, not a personal inbox. This address is BCC'd on every
# desk e-mail and every user notice, and is the fallback recipient when no user
# identity resolves. Empty means nobody on the team sees the negotiations.
FLIGHT_CHANGE_EMAIL_OBSERVER: ""
