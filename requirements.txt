# Core Renglo packages installed from local directories (run from system/).
# Installs latest renglo-lib, renglo-api, and extensions (noma, pes, pes_noma).
# Before deploy: pip install -r requirements.txt then ./zappa_deploy.sh noma_prod update

../dev/renglo-lib
../dev/renglo-api
../extensions/noma/package
../extensions/pes/package
../extensions/pes_noma/package

# Note: The following extensions are commented out as they don't exist yet
# Uncomment and install when available:
# -e ../extensions/enerclave/package
# -e ../extensions/scheduler/package

# Option 2: If these are on GitHub, use git URLs instead:
# git+https://github.com/your-org/renglo-lib.git@main#egg=renglo-lib
# git+https://github.com/your-org/renglo-api.git@main#egg=renglo-api
# git+https://github.com/your-org/noma-mod.git@main#egg=noma-mod

# All other dependencies are specified in the setup.py files above
# and will be installed automatically by pip