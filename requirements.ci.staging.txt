# CI/CD requirements for staging — installs dependency packages from @staging branches
# Used by deploy-staging.yml (production uses requirements.ci.txt @main)

# Process-factory extras that used to arrive via renglo-api. Pin them here so
# Step 8d (and Lambda cold start of application.py) can import create_app
# even when noma-mod's wheel extras are stripped from the freeze file.
openai>=1.0
Flask>=3.0
flask-cors>=4.0
flask-caching>=2.0
flask-cognito>=1.21

git+https://github.com/Noma-Travel/backend.git@staging#egg=noma-mod&subdirectory=package
